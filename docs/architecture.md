# Architecture

This boilerplate is organized around a Cloudflare Workers deployment target with Next.js App Router and
OpenNext. The default app database is Cloudflare D1, with Stack Auth as the authentication source of truth
and Dodo Payments as the billing provider.
