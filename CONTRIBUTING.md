# Contributing

Thanks for helping improve SaaS Boilerplate.

## Local Setup

```bash
bun install
cp .env.example .env.local
cp .dev.vars.example .dev.vars
bun run dev
```

Before opening a pull request, run:

```bash
bun run validate
```

Use small, focused pull requests. Include docs and tests when behavior changes.
